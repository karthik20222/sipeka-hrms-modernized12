# Overtime Feature Tracking

- Added backend overtime entry model, validation controller, and API routes.
- Added frontend overtime logging screen with client-side validation, server error handling, and approval actions.
- Added sidebar and route entry for `/data-overtime`.
- Added SQL schema for `overtime_entries` to the main database dump and left a standalone migration-style file for convenience.
- Use `/Backend/db/employee_payroll_system.sql` or `/Backend/db/employee_payroll_system_overtime.sql` when applying the database change manually.